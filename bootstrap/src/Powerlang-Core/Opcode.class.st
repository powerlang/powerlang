Class {
	#name : #Opcode,
	#superclass : #Object,
	#category : #'Powerlang-Core-Nativization'
}

{ #category : #'as yet unclassified' }
Opcode >> nativizeUsing: anAssembler for: messageNativizer [
	self nativizeUsing: anAssembler
]
